@model Dental_Clinic.Models.ListPrepforTreatment

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ListPrepforTreatment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MedTreatmentid" class="control-label"></label>
                <select asp-for="MedTreatmentid" class ="form-control" asp-items="ViewBag.MedTreatmentid"></select>
            </div>
            <div class="form-group">
                <label asp-for="Medicationid" class="control-label"></label>
                <select asp-for="Medicationid" class ="form-control" asp-items="ViewBag.Medicationid"></select>
            </div>
            <div class="form-group">
                <label asp-for="amountMedications" class="control-label"></label>
                <input asp-for="amountMedications" class="form-control" />
                <span asp-validation-for="amountMedications" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
