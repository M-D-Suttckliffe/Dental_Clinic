@model IEnumerable<Dental_Clinic.Models.MedTreatment>

@{
    ViewData["Title"] = "Index";
}

<h1>Список медецинских лечений</h1>

<p>
    <a asp-action="Create">Создать</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.treatmentName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.treatmentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diagnos.diagnosisName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Обновить</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
